<h1>User#show</h1>

<%= image_tag @user.avatar, { width: 100 } if @user.avatar.attached? %>

<% if @user.id == current_user.id %>
  <br/> <%= link_to 'Edit my profile', edit_user_registration_path %>
<% end %>
